{% layout "server/layouts/base.liquid" %}

{% block content %}
    <div class="paper">
        <h1 class="h1">{{ monster.name }}</h1>
        <img src="{{image}}" alt="" />
        <p>Damage type: {{monster.Damage}}</p>
    </div>
{% endblock %}
